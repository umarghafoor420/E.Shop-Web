import { title } from "process";

export default {
    name: "category",
    type: "document",
    title: "Product Category",
    fields: [
        {
            name: "name",
            type: "string",
            title: "Name of Category"
        }
    ]
}
